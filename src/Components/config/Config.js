
const conf = {
    serviceKey: String(import.meta.env.VITE_SERVICE_KEY),
    templateKey: String(import.meta.env.VITE_TEMPLATE_KEY),
    publicKey: String(import.meta.env.VITE_PUBLIC_KEY)
}

export default conf